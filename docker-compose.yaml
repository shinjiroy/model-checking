services:
  test:
    build: .
    command: npm test

  typecheck:
    build: .
    command: npm run typecheck

  web:
    build: .
    command: npm run dev --workspace=@model-checking/web -- --host 0.0.0.0 --port 5173
    ports:
      - "5173:5173"
