{
  "name": "@model-checking/spec",
  "version": "0.1.0",
  "type": "module",
  "description": "モデル検査DSL(状態・アクション・不変条件の定義)とBFSによる明示的状態探索器",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "bench": "vite-node bench/scaleBenchmark.ts"
  }
}
